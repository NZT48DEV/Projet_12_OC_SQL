name: CI

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - master

jobs:
  tests:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: epic_crm_app
          POSTGRES_PASSWORD: epic_crm_password
          POSTGRES_DB: epic_crm
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U epic_crm_app -d epic_crm"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      EPICCRM_JWT_SECRET: ${{ secrets.EPICCRM_JWT_SECRET }}
      SENTRY_DSN: ""

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install pipenv
        run: python -m pip install --upgrade pip pipenv

      - name: Install dependencies
        run: pipenv install --dev

      - name: Run pre-commit
        run: pipenv run pre-commit run --all-files

      - name: Check required environment variables
        run: |
          test -n "$EPICCRM_JWT_SECRET" || (echo "❌ Missing EPICCRM_JWT_SECRET" && exit 1)
          test -n "$DATABASE_URL" || (echo "❌ Missing DATABASE_URL" && exit 1)
          echo "✅ Required environment variables are set"

      - name: Apply migrations
        run: pipenv run alembic upgrade head

      - name: Run tests
        run: pipenv run pytest -q
